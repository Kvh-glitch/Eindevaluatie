<Window x:Class="AuctionHouse.WPFPresentation.View.PlayerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Players" Height="450" Width="700"
        MinHeight="350" MinWidth="550">
    
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="87*" />
            <ColumnDefinition Width="49*"/>
            <ColumnDefinition Width="204*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,0,10,0" Grid.ColumnSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Text="Players"
                       FontSize="22"
                       FontWeight="Bold"
                       Margin="0,0,0,10"/>

            <ListBox x:Name="PlayerListBox"
                     Grid.Row="1"
                     DisplayMemberPath="Name"
                     SelectionChanged="PlayerListBox_SelectionChanged"/>
        </Grid>

        
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                
                <RowDefinition Height="Auto"/>
                
                <RowDefinition Height="Auto"/>
                
                <RowDefinition Height="*"/>
                
            </Grid.RowDefinitions>

            
            <StackPanel Grid.Row="0"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,0,0,10">
                <Button Content="+ Test item"
                        Width="120"
                        Margin="0,0,10,0"
                        Click="GiveRandomItemButton_Click"/>

                <Button Content="- 1 item"
                        Width="120"
                        Click="RemoveOneItem_Click"/>
                
            </StackPanel>

            
            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        Margin="0,0,0,10">
                <TextBlock Text="Gold: "
                           FontSize="14"
                           FontWeight="Bold"/>
                <TextBlock x:Name="GoldTextBlock"
                           FontSize="14"/>
            </StackPanel>

            
            <TextBlock Grid.Row="2"
                       Text="Inventory"
                       FontSize="16"
                       FontWeight="Bold"
                       Margin="0,0,0,5"/>

            
            <ListBox x:Name="InventoryListBox"
                     Grid.Row="3"/>
            
        </Grid>
    </Grid>
</Window>
