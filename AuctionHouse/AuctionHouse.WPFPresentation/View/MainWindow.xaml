<Window x:Class="AuctionHouse.WPFPresentation.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Auction House" Height="400" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Rarities"
                   FontSize="18"
                   FontWeight="Bold"
                   Grid.Row="0"
                   Margin="0,0,0,10"/>
        <StackPanel Grid.Row="1"
            Orientation="Horizontal"
            Margin="0,0,0,10">
            <Button Content="Show Items"
            Width="120"
            Margin="0,0,10,0"
            Click="ShowItems_Click"/>
            <Button Content="Show Players"
            Width="120"
            Click="ShowPlayers_Click"/>
        </StackPanel>
        <ListBox x:Name="RarityListBox"
                 Grid.Row="2"/>
        <!--DisplayMemberPath="Name"/>-->
    </Grid>
    
</Window>